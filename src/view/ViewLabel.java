package view;

import java.awt.Color;
import java.awt.Graphics2D;

public class ViewLabel {
	/**
	 * This method draws a label for the components
	 * @param g
	 * 		The graphics library used
	 * @param label
	 * 		The label to draw
	 * @param color
	 * 		The color to draw the label in
	 * @throws IllegalArgumentException
	 * 		Illegal label or color
	 */
	private void draw(Graphics2D g, /*Label label, */Color color) {
//		if (label == null || color == null)
//			throw new IllegalArgumentException();
//		if (label.getText() != null)
//			label.setWidth(g);
//		
//		g.setColor(color);
//		g.drawString(label.getText(), label.getX(), label.getY());
//		g.setColor(new Color(0,0,0));
	}
}
