package test;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

import domain.Actor;
import domain.ComponentType;
import domain.Party;
import view.DiagramWindow;

class DiagramWindowTest {
	

	DiagramWindow diagram = new DiagramWindow("GUI");

//	@Test
//	void defaultDiagramTest() {
//		assertEquals(diagram.getDiagramType(), DiagramType.SEQUENCE);
//		assertNotEquals(diagram.getDiagramType(), DiagramType.COMMUNICATION);
//	}
//
//	@Test
//	void changeDiagramViewTest() {
//		assertEquals(diagram.getDiagramType(), DiagramType.SEQUENCE);
//		assertNotEquals(diagram.getDiagramType(), DiagramType.COMMUNICATION);
//		diagram.setDiagramType(DiagramType.COMMUNICATION);
//		assertEquals(diagram.getDiagramType(), DiagramType.COMMUNICATION);
//		assertNotEquals(diagram.getDiagramType(), DiagramType.SEQUENCE);
//	}
	
		
	// Mouse events
	
	// Key events
	
}
